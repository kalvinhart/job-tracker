import styled from "styled-components";
import { InputGroup } from "../../../../common/styles/formStyles";

export const UpdateStatusWrapper = styled.div`
  margin-top: 20px;

  & ${InputGroup} {
    margin-bottom: 40px;
  }
`;
